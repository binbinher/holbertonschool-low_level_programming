#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n:the integer to compute
 * @r: the absolute value
 * Return: Always 0.
 */
int _abs(int n, r)
{
	if (n >= 0)
		r = n if (n < 0)
				r = -n return (0);
}